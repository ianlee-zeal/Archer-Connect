{
    "version": "2.0.0",
    "tasks": [
        {
            // binds to CTRL + SHIFT + B (Build)
            "label": "watch",
            "command": "ng",
            "args": [
                "serve"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "isBackground": true,
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}